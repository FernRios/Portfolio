# Only needed in PRODUCTION. Set this to your deployed API origin, e.g.:
#   VITE_API_BASE=https://fern-portfolio-api.onrender.com
#
# Leave it blank for local development — the Vite dev server proxies /api
# to http://localhost:3000 automatically (see vite.config.ts).
VITE_API_BASE=
